import { AdicionaisPipe } from './adicionais.pipe';

describe('AdicionaisPipe', () => {
  it('create an instance', () => {
    const pipe = new AdicionaisPipe();
    expect(pipe).toBeTruthy();
  });
});
